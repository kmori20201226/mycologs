[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mycologs-ctl"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
mycologs = "mycologs_ctl.main:main"

[tool.setuptools.packages.find]
where = ["src"]
